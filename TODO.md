# What I want to do

Getting familiar with shader programmings in Bevy

## Copy official samples by hand: [Shaders](https://bevy.org/examples-webgpu/#shaders)

- [ ] [Animated](https://bevy.org/examples-webgpu/shaders/animate-shader/)
    > A shader that uses dynamic data like the time since startup. The time data is in the globals binding which is part of the `mesh_view_bindings` shader import.
- [ ] [Extended material](https://bevy.org/examples-webgpu/shaders/extended-material/)
    > Demonstrates using a custom extension to the `StandardMaterial` to modify the results of the builtin pbr shader.
- [ ] [Post processing - Custom render pass](https://bevy.org/examples-webgpu/shaders/custom-post-processing/)
    > This example shows how to create a custom render pass that runs after the main pass and reads the texture generated by the main pass.
- [ ] [Instancing](https://bevy.org/examples-webgpu/shaders/custom-shader-instancing/)
    > A shader that renders a mesh multiple times in one draw call.
    > 
    > Bevy will automatically batch and instance your meshes assuming you use the same `Handle<Material>` and `Handle<Mesh>` for all of your instances.
- [ ] [Render depth to texture](https://bevy.org/examples-webgpu/shaders/render-depth-to-texture/)
    > Demonstrates how to use depth-only cameras.
    > 
    > A depth-only camera is a camera that renders only to a depth buffer, not to a color buffer. That depth buffer can then be used in shaders for various special effects.
- [ ] [Custom render phase](https://bevy.org/examples-webgpu/shaders/custom-render-phase/)
    > This example demonstrates how to write a custom phase
    >
    > Render phases in bevy are used whenever you need to draw a group of meshes in a specific way. For example, bevy's main pass has an opaque phase, a transparent phase for both 2d and 3d. Sometimes, you may want to only draw a subset of meshes before or after the builtin phase. In those situations you need to write your own phase.
    >
    > This example showcases how writing a custom phase to draw a stencil of a bevy mesh could look like. Some shortcuts have been used for simplicity.
    >
    > This example was made for 3d, but a 2d equivalent would be almost identical.
- [ ] [Custom phase item](https://bevy.org/examples-webgpu/shaders/custom-phase-item/)
    > Demonstrates how to enqueue custom draw commands in a render phase.
    >
    > This example shows how to use the built-in `bevy_render::render_phase::BinnedRenderPhase` functionality with a custom `RenderCommand` to allow inserting arbitrary GPU drawing logic into Bevy's pipeline. This is not the only way to add custom rendering code into Bevy—render nodes are another, lower-level method—but it does allow for better reuse of parts of Bevy's built-in mesh rendering logic.

## Other ideas

- Implement some WSL shader examples of anywhere as bevy projects.
- I want to impment
  - Outline shader
  - Drawing a shape behind others
  - a shader using noise
